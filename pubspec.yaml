name: profix_engineering
description: "Quoting app for ProFix Engineering"
publish_to: 'none'
version: 1.0.0

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.5.0
  intl: ^0.19.0

flutter:
  uses-material-design: true
